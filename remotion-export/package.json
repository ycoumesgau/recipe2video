{
  "name": "recipe2video-remotion-export",
  "private": true,
  "type": "module",
  "//": "Documents the runtime deps used by render.mjs. The script is executed from the repo root inside Vercel Sandbox (`node remotion-export/render.mjs`) and resolves these from the root node_modules/, so this file is informational only — no separate npm install runs here anymore.",
  "dependencies": {
    "@remotion/bundler": "4.0.459",
    "@remotion/renderer": "4.0.459",
    "remotion": "4.0.459"
  }
}
